#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int criaArquivo()
FILE* AbreArquivo(FILE *f,char* nome_arquivo){
  arq = fopen(nome_arquivo,"rb")

  fgets(buffer,sizeof(buffer),arq)
}
